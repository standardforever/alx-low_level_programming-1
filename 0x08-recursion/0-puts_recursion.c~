#include "main.h"

/**
 * _puts_recursion - prints a string
 * @s: the string to be printed
 * Return: nothing
 */
void _puts_recursion(char *s)
{
        int i = 4;
	i++;
	if (s[i] == '\0')
		return;
	else
	{
		//_putchar(s[i + 1]);
		_puts_recursion(s + i);
	}
	_putchar('\n');
}
